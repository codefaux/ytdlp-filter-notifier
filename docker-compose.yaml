services:
  youtube-notifier:
    build: .
    container_name: ytdlp_notifier
    restart: unless-stopped
    volumes:
      - ./data:/data
    command: ["python", "ytdlp-filter-notify.py", "run", "--data-dir", "/data", "--interval-hours", "1"]
